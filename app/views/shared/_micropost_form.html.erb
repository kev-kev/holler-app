<%= form_with(model: @micropost, local: true) do |form| %>
  <%= render "shared/error_messages", object: form.object %>
  <div class="field">
    <%= form.text_area :content, placeholder: "what's up, #{current_user.name}?" %>
  </div>
  <%= form.submit "post", class: "btn btn-primary" %>
<% end %>